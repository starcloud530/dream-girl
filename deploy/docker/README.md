# Docker (experimental)

GPU images for the full stack are **not** required for v0.1.0-mvp.
Prefer `deploy/autodl/install.sh` on a bare Linux + NVIDIA host.

A compose skeleton may land in a later release.
